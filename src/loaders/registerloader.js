// src/loaders/registerLoader.js
export const registerLoader = async () => {
  // Simulate a loading delay or fetch data if needed
  return new Promise((resolve) => setTimeout(() => resolve(null), 500));
};
